#!/bin/bash

# Use pgrep to find PIDs of processes matching the name "highlander"
pids="$(pgrep -f highlander)"

# Check if any PIDs were found
if [ -n "$pids" ]; then
  # Use kill to terminate the processes
  kill -9 $pids
else
  echo "No matching processes found to kill."
fi

